{
  "name": "student-complaint-system-monolithic",
  "version": "1.0.0",
  "description": "Production Build Pipeline for Render",
  "scripts": {
    "build": "cd frontend && npm install && npm run build && cd ../backend && npm install",
    "start": "cd backend && node server.js"
  }
}
